services:
  ip:
    image: registry.baileys.dev/ip:latest
    restart: unless-stopped
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.ip.rule=Host(`144.6.89.140`)
      - traefik.http.services.ip.loadbalancer.server.port=8080

networks:
  web:
    external: true
